import 'package:flutter/material.dart';

class ForwardPassword extends StatelessWidget {
  const ForwardPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}